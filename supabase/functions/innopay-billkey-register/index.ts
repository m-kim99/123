// ============================================================
// [폐기됨] 이노페이 즉시등록(빌키) — 앱 내 카드 직접입력 경로. 2026-08-05 무력화
//
// 사유:
//  1) 프론트 호출 0건 — 카드등록은 이노페이 웹링크(innopay-autopay-start/return)로
//     일원화됨(앱은 카드정보를 만지지 않음). 이 함수만 원본 카드정보
//     (cardNum/cardExpire/cardPwd/idNum)를 수신해 불필요한 공격면이었다.
//  2) 규칙 불일치 — 이 경로는 유료 기간을 '등록 시점'부터 시작해 남은 무료 체험
//     기간을 소멸시켰다(웹링크 경로는 체험 만료일부터 이어붙임).
//
// 원본 구현은 git 히스토리 참조. 재도입 시 반드시 웹링크 경로의 기간 규칙
// (잔여 기간 보존)과 동일하게 맞출 것.
// ============================================================
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts';

serve(() =>
  new Response(
    JSON.stringify({ error: 'GONE', message: '더 이상 지원하지 않는 결제 경로입니다.' }),
    { status: 410, headers: { 'Content-Type': 'application/json' } },
  ),
);
